name: flutter_restore
description: Static compatibility scanner for legacy Flutter projects.
version: 1.0.2
repository: https://github.com/Vortex-bit256/flutter_restore
issue_tracker: https://github.com/Vortex-bit256/flutter_restore/issues

topics:
  - flutter
  - android
  - ios
  - gradle
  - migration

platforms:
  linux:
  macos:
  windows:

environment:
  sdk: ^3.8.0

dependencies:
  args: ^2.6.0
  path: ^1.9.1
  pub_semver: ^2.1.5
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15

executables:
  flutter_restore:
